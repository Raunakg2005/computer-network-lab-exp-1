import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Network Bridge - The Unsung Hero of Your LAN",
  description:
    "Learn how Network Bridges intelligently reduce traffic and boost performance in your local area network.",
  keywords: "network bridge, LAN, networking, MAC address, data packets, network performance",
  authors: [{ name: "RKG" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
